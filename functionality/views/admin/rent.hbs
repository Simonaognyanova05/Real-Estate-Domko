{{!-- <h1 class="page-title">Наеми</h1>

<div class="card-apartament">
    {{#each rents}}
    <div class="card" style="width: 18rem;">
        <img src="{{this.img1}}" class="card-img-top" alt="..." style="width: 100%; height: 180px">
        <div class="card-body">
            <h5 class="card-title">{{this.type}}</h5>
            <p class="card-text">гр./с. {{this.location}}</p>
            <p class="card-text">Цена: {{this.price}}</p>
            <p class="card-text">Цена за оглед: {{this.priceForHour}}</p>

            <a href="/admin/updateRent/{{this._id}}" class="btn btn-primary">Редактиране</a>
            <a href="/deleteRent/{{this._id}}" class="btn btn-primary">Изтриване</a>
        </div>
    </div>
    {{else}}
    <p>Няма създадени имоти под наем</p>
    {{/each}}
</div>

<div id="create-rent">
    <a href="/admin/createRent" class="btn btn-primary">Създаване на имот под наем</a>
</div> --}}

<section class="container" style="margin-top: 150px;" id="popular">

    <div class="d-flex">
        <h2>Наеми</h2>
    </div>
    <p class="pt-5"><a href="" id="create-label"><i class="fa-solid fa-plus"></i> Добави имот</a></p>


    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4 mt-2">
        {{#each rents}}
        <div class="col">
            <div class="card border-0 rounded-0">
                <img src="{{this.img1}}" alt="image1" class="card-img-top rounded-0" style="height: 300px;">
                <div class="card-body">
                    <p class="card-text text-muted">{{this.type}}</p>
                    <p class="card-text text-muted">гр./с. {{this.location}}</p>


                    <div class="d-flex justify-content-center mt-3">
                        <p class="mx-4 text-muted card-text"><i class="fa-solid fa-money-bill"></i> Цена за оглед:
                            {{this.priceForHour}} лв.</p>
                        <p class="mx-4 text-muted card-text"><i class="fa-solid fa-money-bill"></i> Цена:
                            {{this.price}} лв.</p>
                    </div>

                    <div class="d-flex my-2">
                        <a href="/admin/updateRent/{{this._id}}"
                            class="btn btn-lg text-white bg-dark px-4 rounded-0 edit-delete">Редактиране</a>
                        <a href="/deleteRent/{{this._id}}"
                            class="btn btn-lg text-white bg-dark px-4 rounded-0 edit-delete">Изтриване</a>
                        {{!-- <button class="btn btn-lg text-white bg-dark px-4 rounded-0"
                            style="margin: 10px;">Редактиране</button>
                        <button class="btn btn-lg text-white bg-dark px-4 rounded-0"
                            style="margin: 10px;">Изтриване</button> --}}
                    </div>
                </div>
            </div>
        </div>
        {{else}}
        <p>Няма създадени имоти под наем</p>
        {{/each}}


    </div>

</section>